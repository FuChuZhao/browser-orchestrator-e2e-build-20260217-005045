# Browser Orchestrator Build Repo

This public repository contains only GitHub Actions workflows.
It clones the private source repository via SSH deploy key and runs:

- functional E2E automation (including real extension load for Chrome/Firefox)
- narrative screenshot/story generation

Artifacts are retained for 1 day.
